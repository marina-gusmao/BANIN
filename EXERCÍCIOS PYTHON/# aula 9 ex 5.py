# aula 9
#ex 5
N = int(input())
A = []
POS = []
NEG = []
cont = 0
if N > 0 and N <= 50:
    for i in range(N)
        Nu = float(input(f' elemento {i}: '))
        if Nu >= 0:
            POS.append(Nu)
        elif Nu < 0:
            NEG.append(Nu)
    A = POS + NEG
        cont += 1
print(f'\nLista de negativos: {LstNeg}, contém {len(LstNeg)} elementos')
print(f'Lista de positivos: {LstPos}, contém {len(LstPos)} elementos')
print('\nFim do Programa')

